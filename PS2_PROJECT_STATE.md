<!-- ⛔ RULES — Re-read this section EVERY TIME you open this file. -->
<!-- These rules also appear in the SKILL.md. Redundancy is intentional. -->

# ⛔ QUICK RULES (mandatory re-read)

1. **Build:** `cmake --build <build_dir>` — NEVER add `--clean-first`, `--target clean`, or delete the build directory.
2. **Files:** NEVER modify `runner/*.cpp`. Fix in `src/lib/` or game overrides.
3. **Headers:** NEVER modify `.h` without asking user. Triggers mass rebuild.
4. **Git:** NEVER use destructive git commands (`checkout`, `clean`, `reset`, `stash`, `pull`).
5. **Verify:** NEVER assume file names/paths. Use tools (`list_dir`, `find_by_name`, `grep_search`).

---

# PS2 Recomp — Project State
> Auto-maintained by agent. DO NOT DELETE. Read at session start, update after every major action.

## Boot Status
- [x] Loaded `03-ps2recomp-pipeline.md`
- [x] Loaded `04-runtime-syscalls-stubs.md`
- [x] Verified comprehension (3 questions answered)

## Game Info
- **Title**: Need for Speed: Hot Pursuit 2 Alpha
- **Region**: NTSC-U
- **Has Symbols**: partial

## Workspace Paths
- **PS2Recomp Repo**: `D:/Repos/Recomp/PS2Recomp`
- **Game Workspace**: `F:/Games/ISO/PS2/ALPHA`
- **ISO Path**: `F:/Games/ISO/PS2/ALPHA`
- **Output Dir**: `D:/Repos/Recomp/PS2Recomp/output`

## Binaries
| Role | File Name | Type | Path | TOML Config | Status |
|------|-----------|------|------|-------------|--------|
| Main | SLUS_203.62 | ELF | `F:/Games/ISO/PS2/ALPHA/SLUS_203.62` | `F:/Games/ISO/PS2/ALPHA/SLUS_203.62.toml` | recompiled + built |
| CD/DVD reference | 3AFE35FF.elf | ELF | `F:/Games/ISO/PS2/ALPHA/3AFE35FF.elf` | `F:/Games/ISO/PS2/ALPHA/3AFE35FF.toml` | reference only |
| Fallback analysis | SLUS_203.62_ps2xAnalyzer.toml | TOML | `F:/Games/ISO/PS2/ALPHA/SLUS_203.62_ps2xAnalyzer.toml` | `F:/Games/ISO/PS2/ALPHA/SLUS_203.62_ps2xAnalyzer.toml` | reference only |

## Environment Setup
- **Ghidra Install Path**: `D:/Development/Ghidra`

## Current Phase
PHASE_RUNTIME_BUILD

## Build Configuration
- **CMake Generator**: Visual Studio 18 2026
- **C++ Compiler**: MSVC 14.50.35717
- **Build Type**: Debug
- **Ghidra CSV Path**: `F:/Games/ISO/PS2/ALPHA/SLUS_203.62_cm.csv`
- **single_file_output**: false

## Active Runner Command
<!-- ACTIVE RUNNER COMMAND: -->
`D:/Repos/Recomp/PS2Recomp/output/build/Debug/ps2_game.exe "F:/Games/ISO/PS2/ALPHA/SLUS_203.62"`

## Unique Crashes & Subsystem Map
| Crash Address/PC | Subsystem | Callstack/Context | Proposed Fix Type | Regression Status | Resolution |
| ---------------- | --------- | ----------------- | ----------------- | ----------------- | ---------- |
| 0x125AE0 / 0x125C40 | bootstrap / kernel syscalls | Legacy monitor syscall `0x63` (`$a0=0x15`) triggered during platform probe | Runtime C++ | fixed in current build | handled as unavailable / returns 0 |
| 0x230210 / 0x234F90 / 0x230218 | libc heap stubs | `__malloc_lock`, `__malloc_update_mallinfo`, `__malloc_unlock` fired during allocator init | Runtime C++ | fixed in current build | no-op triage handlers added |
| 0x231348 | stdio | `vfprintf` received guest pointer-like file handle during shutdown/logging | Runtime C++ | fixed in current build | guest-handle fallback to `stdout` |
| 0x1FA7A0 | generated control flow | `RecurseDirectory` recursive tail path returned to internal label `0x1FA7A0`, which dispatcher treated as missing function | Game Override / runtime dispatch | fixed in current build | ALPHA-only continuation handlers plus late function-table re-sort keep continuation PCs visible to lookup |
| 0x2264C0 / 0x1FA4D0 | CD streaming | `sceCdRead` receives unresolved sector request and game loops in fatal CD retry | Runtime C++ / game data-flow | active | current primary blocker |

## Resolved Stubs
| Address | Handler | Binding Method | Status | Notes |
| ------- | ------- | -------------- | ------ | ----- |
| export-derived | 395 named bindings | `F:/Games/ISO/PS2/ALPHA/SLUS_203.62.toml` stubs array | active | ExportPS2Functions.java classification is the authoritative baseline |

## Resolved Syscalls
| Syscall ID | Implementation | File | Status |
| ---------- | -------------- | ---- | ------ |
| 0x40 | CreateSema | `ps2xRuntime/src/lib/ps2_syscalls.cpp` | observed working |

## Temporary Triage Stubs
| Address | Triage Type | Caller Context | Priority | Notes |
| ------- | ----------- | -------------- | -------- | ----- |
| 0x223C08 | named stub | `InitAlarm` via syscall override path | high | still logs as unimplemented during boot |
| 0x125BF0 | named stub | `__main` | medium | boot continues past warning, but startup still logs it |

## Unhandled Opcodes
| Address | Opcode | Type | Resolution |
| ------- | ------ | ---- | ---------- |
| none | none | scan result | `rg "Unhandled opcode" D:/Repos/Recomp/PS2Recomp/output` returned no matches after ALPHA recompilation |

## Known Issues
- [x] ALPHA export TOML was patched to emit generated C++ into `D:/Repos/Recomp/PS2Recomp/output` instead of `F:/Games/ISO/PS2/ALPHA/output`.
- [x] `output/main.cpp` now defaults to `F:/Games/ISO/PS2/ALPHA/SLUS_203.62`.
- [x] `ps2_recomp` successfully regenerated 8,704 functions into repo `output/`.
- [x] `output/build/Debug/ps2_game.exe` was rebuilt at `2026-03-16 17:52:59`.
- [x] Runtime bootstrap no longer dies at internal PC `0x1FA7A0`; ALPHA game override continuation handlers are active.
- [x] `__18TempDirectoryEntryiiPCcT3` is no longer an unimplemented stub; ALPHA override now allocates/copies the guest path string and stores entry metadata.
- [x] `output/main.cpp` now pins the ALPHA prototype disc image: `F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002 (prototype).iso`.
- [x] `__10AsyncEntryP5bFilePFP5bFileP10AsyncEntry_i` now has a runtime constructor implementation in `ps2xRuntime/src/lib/ps2_stubs.cpp`.
- [ ] Boot still enters repeated CD-error recovery because `sceCdRead` at `0x2264C0` receives an unresolved request (`ra=0x1FA4D0`, `a0=0x1fe8caa`, `a1=0x1`, `a2=0x1fe8520`).
- [ ] Need to trace the directory/file-state path feeding `ReadCDSectorASync__FPvii_0x1fa4a0`; `bFile` ctor fix, `AsyncEntry` ctor fix, and explicit ALPHA ISO binding did not correct the bogus LBN path.
- [ ] Build wrappers left an idle `MSBuild` process after link; artifact output is valid, but exit-code confirmation from the wrapper remains noisy.
- [ ] `0x1FA7A0` continuation dispatch is fixed. The active blocker is again CD I/O: after `Loading entire directory...`, runtime loops on `sceCdGetError() == -1` / `FatalCDErrorFunction()` and logs `[sceCdRead] unresolved request pc=0x2264c0 ra=0x1fa4d0 a0=0x1070036 a1=0x3181 a2=0x7282a0`.

## Learned Patterns (Auto-growing)
- For this repo, the safest “from scratch” path is to keep the exported game TOML as the source of truth and only redirect its `output` path into the repo build target.
- A clean ALPHA baseline means: `ps2_recomp` emits 8,704 functions, `register_functions.cpp` is regenerated, no `Unhandled opcode` markers remain, and `output/build/Debug/ps2_game.exe` relinks against the new export.
- The first executable boot proves the rebuild path is valid even before gameplay works; the next blockers are runtime-layer issues, not recompiler codegen failures.
- For NFS HP2 Alpha, one generated recursive function (`RecurseDirectory`) leaks internal resume PCs to the dispatcher. The safe fix is an ELF-specific game override in `game_overrides.cpp`, not editing generated output.
- `TempDirectoryEntry` in this build needs game-specific construction logic: guest string duplication plus metadata writes. Leaving the export stub in place corrupts later file-system state.
- The default CD auto-detection can select the wrong parent-directory image. For this title, `paths.cdImage` must be pinned to the ALPHA prototype ISO instead of relying on auto-detect.
- The async filesystem queue uses a real `AsyncEntry` object with a stored worker proc at offset `0x34` and a file reference at `0x08`; leaving that constructor as a TODO stub is unsafe even if boot appears to progress.
- The unresolved CD read survives the `AsyncEntry` constructor fix, which points back to directory/path data-flow rather than the queue object alone.
- The current ALPHA runtime is also sensitive to media-path normalization: `DIR.BIN` must resolve from the effective `cdRoot` / `cdImage` configuration before directory-entry tracing can even reach `OpenCDFile`.
- `3AFE35FF.elf` appears to be a CD/DVD-heavy subsystem binary: its exported map begins with `sceCdRead`, `sceCdGetError`, `sceCdSearchFile`, `sceCdDiskReady`, and related helpers. Treat it as reference material for the active CD failure, not as a replacement primary runner target.
- The unresolved `sceCdRead` request currently logged from the main build (`a0=0x1070036 a1=0x3181 a2=0x7282a0`) does not match the simple LBN/count contract used by the one-sector async path. A guarded byte-range fallback was added to test the hypothesis that some callers leak absolute byte offset + byte count into the CD read stub.
- The HP2 unpacked asset tree at `F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002_OUT_Unpacker/ZZDATA` is a better runtime data source than continuing to guess low-level `DATA.BIN` read contracts. The next runtime iteration remaps `DIR.BIN` entries onto pseudo-LBNs backed by extracted host files.
- A matched game override descriptor is not sufficient if `registerFunction()` appends duplicate addresses. `lower_bound()` can still resolve to the older generated/stub entry first. Override-address replacement must be explicit for addresses like `0x1FA548` and `0x1FA888`.
- The reboot failure path is not a raw SIF reboot issue. `bLoadIRXModule__FPc_0x1f9f88` fails when `sceSifLoadModule` receives malformed or empty module paths, and the missing `%s` formatter callbacks (`_str_out_char`, `_str_out_str`) sit directly on that path.
- The current ALPHA boot gets past IOP reboot, IRX loading, and `DIR.BIN` remap. The visible hang is now a live-thread / blank-frame state: the host window keeps rendering black frames while the guest thread stays active and the last exported debug PC remains `0x125AE0`.
- The latest logs show `sceGsSyncVCallback` registers `PSX2VBlank` at `0x100038`, and the new diagnostics confirm that the VSync worker starts and repeatedly dispatches the callback. The next uncertainty is inside the callback itself: additional return-state logging was added to show whether `PSX2VBlank` returns cleanly, stalls internally, or hands off to another bad PC.
- `PSX2VBlank` returning in one host step is expected for the generated callback body. The stronger signal is that sampled `run:tick` state stays at `_start` (`0x125AE0`) while one guest worker thread remains active, so runtime tick diagnostics need to expose live dispatch-loop worker PCs instead of only the bootstrap context.
- The first `workerPcs=` patch confirmed there is only one active dispatch thread, but it still showed `_start` because snapshots were taken before entering a long nested guest call chain. `run:tick` now prefers live `m_cpuContext` register reads so the next log should report the actual parked guest PC instead of the stale pre-dispatch entry.
- Live `run:tick` sampling now shows the real parked PC in a black-screen state: `0x14021C/0x140228/0x14022C`, which maps to `FindEventNode__8Joystick8JoyEvent_0x140208`. That loop walks a circular joystick event list and is not reaching its sentinel, so the next override targets list-cycle detection instead of GS or VBlank delivery.

## Session Log
### 2026-03-16
- **Actions**: Verified `F:/Games/ISO/PS2/ALPHA/SLUS_203.62`, `SLUS_203.62.toml`, and `SLUS_203.62_ps2xAnalyzer.toml`; patched the exported TOML output path to repo `output/`; updated the runner default ELF path; re-ran `ps2_recomp`; scanned for unhandled opcodes; rebuilt `output/build`; smoke-tested the fresh executable against the ALPHA ELF.
- **Discovered**: The exported TOML is richer than the analyzer fallback and is the right baseline. Recompilation succeeded with 8,704 functions and no unhandled opcodes. The rebuilt executable initializes raylib, loads the ELF, creates semaphores, and reaches the guest entry point before stalling.
- **Current Blocker**: Runtime bootstrap is blocked by unknown syscall `0x63`, heap-related stubs (`__malloc_lock`, `__malloc_update_mallinfo`, `__malloc_unlock`), and an invalid `vfprintf` file handle during shutdown.
- **Actions**: Added runtime handling for syscall `0x63`, triaged malloc lock/mallinfo stubs, added `vfprintf` guest-handle fallback, raised guest heap hard limit to full RAM, then added ALPHA-specific game overrides in `ps2xRuntime/src/lib/game_overrides.cpp` for leaked `RecurseDirectory` continuation PCs and `TempDirectoryEntry` construction. Rebuilt both `out/build` and `output/build` under `vcvars64` after each patch and reran `ps2_game.exe`.
- **Discovered**: The previous crash at `0x1FA7A0` is resolved. The `TempDirectoryEntry` constructor stub warning is gone. Boot now progresses into CD/file streaming, where `sceCdRead` still receives an unresolved request (`pc=0x2264C0`, `ra=0x1FA4D0`, `a0=0x1fe8caa`, `a1=0x1`, `a2=0x1fe8520`) and the game loops in fatal CD-error handling.
- **Current Blocker**: Trace the file-object sector-base path that feeds `ReadCDSectorASync__FPvii_0x1fa4a0` / `sceCdRead_0x2264C0`. Current evidence says the remaining failure is in CD/file-state decoding, not recompiler codegen or the previous recursive dispatch bug.
- **Actions**: Implemented the missing `__10AsyncEntryP5bFilePFP5bFileP10AsyncEntry_i` runtime constructor in `ps2xRuntime/src/lib/ps2_stubs.cpp`, pinned `paths.cdImage` in `output/main.cpp` to `F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002 (prototype).iso`, rebuilt `out/build` and `output/build` under `vcvars64`, and reran the ALPHA executable with live process capture.
- **Discovered**: The game now uses the ALPHA prototype ISO instead of auto-detecting the retail disc image, and the async queue object is no longer a TODO stub. Despite that, the same unresolved CD request persists (`pc=0x2264C0`, `ra=0x1FA4D0`, `a0=0x1fe8caa`, `a1=0x1`, `a2=0x1fe8520`). Live tracing also shows watched path-buffer memory around `0x1efffa0` being allocator-poisoned (`0xEE` then `0xAA`) immediately before `Loading entire directory...`, which strengthens the case that the remaining bug is in directory/path construction rather than the raw `sceCdRead` implementation.
- **Current Blocker**: Trace the directory-load path from `MakeSureDirectoryIsLoaded` / `LoadDirectory` / `RecurseDirectory` into `OpenCDFile` and confirm whether the directory-entry metadata or caller path hash is malformed before `sceCdRead` is reached.
### 2026-03-20
- **Actions**: Added ALPHA-only trace wrappers in `ps2xRuntime/src/lib/game_overrides.cpp` for `OpenCDFile__FPiT0PCc_0x1faba8` and `ReadCDSectorASync__FPvii_0x1fa4a0`, plus extra media-path fallback logic for `DIR.BIN` resolution. Rebuilt incrementally with `cmd.exe /c "call ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\VC\Auxiliary\Build\vcvars64.bat"" && cmake --build output\build"` and reran `output\Debug\ps2_game.exe F:\Games\ISO\PS2\ALPHA\SLUS_203.62`.
- **Discovered**: The incremental build is healthy, but runtime now fails before the intended CD-read trace becomes useful. `LoadDirectory` still logs `DIR.BIN missing at F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002 (Mar 6, 2002 prototype)/DIR.BIN`, while the actual file on disk is `F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002/DIR.BIN`. That means the current blocker has moved one step earlier to media-path normalization.
- **Current Blocker**: Fix or fully expose the effective ALPHA `cdRoot` / `cdImage` normalization used by `LoadDirectory`; once `DIR.BIN` resolves from the real on-disk path, rerun and use the new `OpenCDFile` / `ReadCDSectorASync` wrappers to continue tracing the original `sceCdRead` path corruption.
### 2026-03-21
- **Actions**: Fixed the staged build wrapper so `output/build` removes the stale `output/ps2_runtime.h` before compiling, which restored successful `ps2_game.exe` builds. Updated `build.ps1 -Action run` to write raw process output to `output/Debug/log.txt` automatically, and added game-override probe logging in `ps2xRuntime/src/lib/game_overrides.cpp`.
- **Discovered**: The latest raw run log still fails on `Function at address 0x1FA7A0 not found`. Inspecting `output/RecurseDirectory__FPt6bTList1Z18TempDirectoryEntryUiUiPCci_0x1fa648.cpp` shows `0x1FA7A0` is an internal label inside `RecurseDirectory`, not a standalone exported function. `ps2xRuntime/src/lib/game_overrides.cpp` already registers `0x1FA7A0` to `nfsHp2AlphaRecurseDirectoryContinuation`, so the remaining question is whether the ALPHA override descriptor is matching at runtime.
- **Current Blocker**: Rebuild runtime/output and inspect `output/Debug/log.txt` for the new `[game_overrides] probe ...` line. If there is no override match for `SLUS_203.62`, trace the ELF-name/entry mismatch. If there is a match, continue investigating why dispatch still misses the continuation PC.
- **Actions**: Verified the new `raw_log.txt` capture path through `build.ps1 -Action run`. Confirmed from `output/Debug/raw_log.txt` that the ALPHA game override is applied for `SLUS_203.62`, then patched `ps2xRuntime/src/lib/ps2_runtime.cpp` so `registerFunction()` re-sorts the function table when late override registrations append lower addresses after the initial `finalizeFunctionTable()` call.
- **Discovered**: The earlier `0x1FA7A0 not found` failure was not an override-match problem. The actual hazard is ordering: `registerAllFunctions(runtime); runtime.finalizeFunctionTable();` runs before `PS2Runtime::loadELF()`, while `ps2_game_overrides::applyMatching()` runs inside `loadELF()` and appends the continuation handlers afterward. That leaves `m_functionTable` unsorted for `lower_bound`-based lookup unless late registrations trigger a re-sort.
- **Actions**: Rebuilt and reran after the late-registration re-sort patch. `output/Debug/ps2_register_trace.txt` now records `resorted function table after late registration for address=0x1fa7a0`, and the run no longer reports `Function at address 0x1FA7A0 not found`.
- **Discovered**: With continuation dispatch fixed, runtime progresses into the original CD-loading failure again. `output/Debug/raw_log.txt` shows `Loading entire directory...`, then repeated `sceCdGetError() returned error code -1` / `FatalCDErrorFunction()` loops, plus `[sceCdRead] unresolved request pc=0x2264c0 ra=0x1fa4d0 a0=0x1070036 a1=0x3181 a2=0x7282a0`.
- **Current Blocker**: Resume tracing the directory/file-state path feeding `ReadCDSectorASync__FPvii_0x1fa4a0` / `sceCdRead_0x2264c0`. The remaining bug is back in CD/path data flow rather than function registration or continuation dispatch.
- **Actions**: Identified a second extracted ELF at `F:/Games/ISO/PS2/ALPHA/3AFE35FF.elf` with companion files `3AFE35FF.toml`, `3AFE35FF.csv`, and `3AFE35FF_ps2xAnalyzer.toml`. Inspected the ELF header and export map.
- **Discovered**: `3AFE35FF.elf` is an ELF32 MIPS executable with entry `0x00200008`. Its CSV/TOML map is dominated by CD/DVD functions such as `sceCdRead`, `sceCdGetError`, `sceCdSearchFile`, `sceCdDiskReady`, and `sceCdStatus`, making it directly relevant to the current unresolved CD read loop.
- **Actions**: Compared the generated main-ELF CD wrappers against the unresolved runtime request. `AsyncRealCDRead__FP5bFileP10AsyncEntry_0x1fadc0` uses the expected one-sector pattern, but the live unresolved request from `ReadCDSectorASync__FPvii_0x1fa4a0` (`a0=0x1070036 a1=0x3181 a2=0x7282a0`) does not fit that contract. Added a guarded byte-range fallback in `ps2xRuntime/src/lib/stubs/ps2_stubs_ps2.inl` to test `a0=byteOffset, a1=byteCount, a2=dst`.
- **Current Blocker**: Rebuild and rerun to see whether `sceCdRead` now recovers with the byte-range fallback. If it does not, instrument the direct `ReadSector__FPvii_0x1fa510` callers and identify the per-caller argument contract.
- **Actions**: Inspected the unpacker/modelulator workflow used on `HP2_A098_PS2_07_MAR_2002`, confirmed that `NFS.Unpacker` is driven by `ZDIR.BIN`, and verified the extracted asset tree under `F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002_OUT_Unpacker/ZZDATA`. Patched `ps2xRuntime/src/lib/stubs/helpers/ps2_stubs_helpers.inl` to prefer auto-detected `ZZDATA` roots for host-file registration, added `ps2_stubs::registerCdHostFileAlias(...)` in `ps2xRuntime/src/lib/ps2_stubs.cpp`, and updated the ALPHA `LoadDirectory` override in `ps2xRuntime/src/lib/game_overrides.cpp` to rewrite matching `DIR.BIN` entries onto pseudo-LBNs backed by extracted files.
- **Discovered**: The runtime already had enough structure to use extracted host files without changing generated code or widening `sceCdRead` heuristics further. The missing piece was a hash bridge: uppercase/backslash NFS BIN path hashing for known `ZZDATA` files plus `__Unknown_Resolved/inventory.csv` for unresolved hash-backed assets.
- **Actions**: Compared the fresh logs against the new override code and found that `[game_overrides] applied 1 matching override(s).` was present, but runtime still executed the old `TempDirectoryEntry` stub and never logged `loaded DIR.BIN override entries=... remapped=...`. Patched `ps2xRuntime/src/lib/ps2_runtime.cpp` so `registerFunction()` replaces an existing same-address entry instead of appending a duplicate.
- **Discovered**: The ALPHA override descriptor was matching correctly, but same-address registrations such as `0x1FA548` and `0x1FA888` were competing with earlier generated/stub registrations. `lower_bound()` could still return the original binding first, which explains why override matching and old stub behavior coexisted in the same run.
- **Current Blocker**: Rebuild `ps2_runtime` and `ps2_game`, then rerun and verify that duplicate-address replacements now activate the ALPHA `TempDirectoryEntry` / `LoadDirectory` overrides and produce the `remapped=` startup log.
- **Actions**: Confirmed from `output/Debug/log.log` that the ALPHA override now fully remaps `DIR.BIN` (`entries=404 remapped=404`) against `F:/Games/ISO/PS2/HP2_A098_PS2_07_MAR_2002_OUT_Unpacker/ZZDATA`. Traced the next failure to the IOP reboot/module-load path by inspecting `bRebootPS2__FPcT0PPci_0x1fa1e8.cpp`, `bLoadIRXModule__FPc_0x1f9f88.cpp`, `bVSPrintf__FPcPCcT0_0x1f6570.cpp`, and `_bOutput__FcPCcPc_0x1f6620.cpp`. Implemented `_str_out_char__FcPi` and `_str_out_str__FPCciPi` in `ps2xRuntime/src/lib/ps2_stubs.cpp` so the string-output formatter advances a mutable guest output pointer instead of falling back to `TODO_NAMED`.
- **Discovered**: `Reboot failed!` is downstream of repeated `sceSifLoadModule` failures, and those failures follow `_str_out_str__FPCciPi` warnings in the formatter path. `_bOutput` passes a mutable output-pointer cell at `a2` for `%s` and `a1` for `%c`, so the missing callbacks only needed to write bytes into the destination buffer and advance that cell. The new runtime implementation should restore real IRX path construction without touching generated code.
- **Actions**: Reviewed the next `output/Debug/log.log` after the formatter patch. `_str_out_str__FPCciPi` warnings are gone, but `WARNING:  gave error -1 (unknown)` and `ERROR:  Reboot  failed!` still occur before the `DIR.BIN` remap log. Added failure diagnostics in `ps2xRuntime/src/lib/syscalls/ps2_syscalls_rpc.inl` so `SifLoadModule` now logs either an empty guest path (with a 16-byte preview) or a rejected normalized path.
- **Discovered**: The follow-up run disproved that first pointer-sync hypothesis. `sceSifLoadModule` still logs `empty path` with preview `00 48 00 ...`, and inspecting `_bOutput__FcPCcPc_0x1f6620.cpp` shows why: `_str_out_char__FcPi` and `_str_out_str__FPCciPi` do not receive an output-pointer cell at all. Their `Pi` argument is a count accumulator, while the actual current string destination lives in the runtime global at `gp-0x47FC`.
- **Actions**: Rewrote `_str_out_char__FcPi` and `_str_out_str__FPCciPi` in `ps2xRuntime/src/lib/ps2_stubs.cpp` to match that calling contract. They now write through the global output pointer at `gp-0x47FC` and increment the passed-in count accumulator instead of treating it as an output pointer.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun again. The next decisive check is whether `SifLoadModule` still reports `empty path`; if not, reboot should either succeed or narrow to a concrete module path/translation problem.
### 2026-03-22
- **Actions**: Reviewed `output/Debug/log.txt` together with `output/PSX2VBlank__Fi_0x100038.cpp`, `output/ReadJoystickData__Fv_0x118fd0.cpp`, and `output/eVblCallback__Fv_0x10df28.cpp`. Confirmed that `PSX2VBlank` is a straight-line generated callback and patched `ps2xRuntime/src/lib/ps2_runtime.cpp` to track per-dispatch-thread snapshots (`pc`, `ra`, `sp`, `gp`, stable-iteration count) directly from `dispatchLoop()`. The run-loop `[run:tick]` log now includes a compact `workerPcs=` summary instead of only the bootstrap `ctx`.
- **Discovered**: The previous `steps=1` callback return is not itself the bug. The more useful signal is the persistent `_start` sample (`pc=0x125AE0`) plus `activeThreads=1` during a black screen, which means the live guest logic is likely parked in another dispatch loop. The new snapshot logging is aimed at exposing that real worker PC on the next run.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun so `output/Debug/log.txt` includes the new `workerPcs=` field. The next diagnosis depends on the actual parked guest worker PC(s), not the bootstrap `_start` sample.
- **Actions**: Reviewed the next `output/Debug/log.txt` after the worker snapshot patch. It showed `workerPcs=#1:pc=0x125ae0...`, which proved there is only one live dispatch thread, but also revealed the snapshot was still stale because it was recorded before entering a long nested guest call chain. Updated `ps2xRuntime/src/lib/ps2_runtime.cpp` so `[run:tick]` prefers live `m_cpuContext` register reads (`pc`, `ra`, `sp`, `gp`) instead of only the pre-dispatch debug atomics.
- **Discovered**: The persistent `_start` sample was a logging artifact, not enough evidence that the guest is truly parked at `_start`. The next rerun needs the new live register sampling to expose the actual deep guest PC while the black-screen thread is still active.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun again. The next useful datum is the live parked PC reported by the updated `[run:tick]` line.
- **Actions**: Reran with the live `run:tick` sampler and mapped the parked PC sequence `0x14021C -> 0x140228 -> 0x14022C` to `FindEventNode__8Joystick8JoyEvent_0x140208`, called from `AddJoyHandler__F12JoystickPort8JoyEventPF12JoystickPort8JoyEventUcPv_vPv_0x140778`. Added an ALPHA override in `ps2xRuntime/src/lib/game_overrides.cpp` for `0x140208` that preserves the normal sentinel search but detects circular-node revisits and runaway traversal, logs the corrupt list state, and returns `null` instead of hanging forever.
- **Discovered**: The black-screen/live-thread state is no longer an opaque scheduler issue. It is a concrete infinite traversal in the joystick event-node list during `AddJoyHandler`, before the later node allocation path at `0x1FC530`.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun again. The next log should either report `[nfs-hp2-alpha] joystick event list cycle detected ...` / `runaway ...` or move past `FindEventNode` into the next startup boundary.
- **Actions**: Traced the post-joystick crash path through `bServiceFileSystem__Fv_0x1f8ae8`, `AddQueuedFile__FPvPCciiPFii_vi_0x182020`, `BeginRead__10QueuedFile_0x181d20`, `ServiceQueuedFiles__Fv_0x181e10`, and the generated TODO constructors for `QueuedFile`, `ResourceFile`, and `TexturePack`. Replaced the runtime `TODO_NAMED` implementations in `ps2xRuntime/src/lib/ps2_stubs.cpp` with concrete constructors for `__10QueuedFilePvPCciiPFii_vi`, `__12ResourceFilePCc16ResourceFileTypeiPFi_vi`, and `__11TexturePackPCc`.
- **Discovered**: The strongest current corruption risk is the uninitialized `QueuedFile` callback/state block. `ServiceQueuedFiles` executes the function pointer at `this+0x50`, reads user data from `this+0x54`, uses `this+0x58` as an id, and branches on `this+0x5C/0x60`. Leaving the constructor as a TODO can plausibly turn filename bytes like `.BIN` into a `jalr` target. `ResourceFile` and `TexturePack` were also still emitted as TODO constructors, so their list/type/sentinel fields were being left as garbage.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun to verify whether the `.BIN`-as-code failure is gone. The decisive next evidence is the first new `output/Debug/log.txt` after these constructor fixes, especially whether the old `Function at address 0x4e49422e not found` warning disappears or moves to a different boundary.
- **Actions**: Reviewed the next `output/Debug/log.txt` after the constructor patch. The old `0x4e49422e` bad-PC is gone. The run now reaches `OpenPlatformFile("GLOBAL\\DYNTEX.BIN")`, then spins in `bIsAsyncDone__FP5bFile_0x1f9938 -> bServiceFileSystem__Fv_0x1f8ae8`. Re-checked the generated async file paths (`bIsAsyncDone`, `bReadAsync`, `_$_10AsyncEntry`) and corrected the `bFile` constructor in `ps2xRuntime/src/lib/ps2_stubs.cpp` so `bFile+0x74` starts at `0` instead of the queue sentinel address.
- **Discovered**: `bFile+0x74` is a pending async-operation count, not a sentinel pointer. `bIsAsyncDone` returns complete only when that field reaches zero, and `_$_10AsyncEntry` decrements it on teardown. Seeding it with the global queue sentinel guaranteed a permanent “async not done” state even after successful file open/read setup.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun again. The next useful check is whether the `GLOBAL\\DYNTEX.BIN` path now advances past the `bIsAsyncDone` poll loop into the next visible loading/gameplay boundary.
- **Actions**: Reworked the build wrapper for incremental `output` builds. `output/CMakeLists.txt` now groups generated `output/*.cpp` into chunked object-library targets (`PS2X_OUTPUT_CHUNK_SIZE`, default `256`) and links `ps2_game` from those chunks instead of one monolithic source list. `build.ps1` now supports `-Action smart` routing plus `-OutputBackend auto|vs|ninja`; the smart path auto-skips no-op builds and can route `output` builds to a separate `output/build-ninja` directory when Ninja is available.
- **Discovered**: This environment currently does **not** have `ninja` installed, so only the MSBuild/Visual Studio backend is locally validated right now. The chunked-target split still helps the Visual Studio path because unchanged object-library chunks can remain up to date instead of treating the generated runner as one monolithic target.
- **Current Blocker**: Validate the new chunked `output` build on the next local compile and, if faster output builds are still needed, install Ninja so `.\build.ps1 -Action smart -OutputBackend ninja` can be exercised.
- **Actions**: Verified that Ninja is now installed, inspected the failed `output/build-ninja` configure state, and patched `build.ps1` so the Ninja backend no longer uses `Ninja Multi-Config`. The wrapper now prefers Visual Studio's bundled `ninja.exe`, creates single-config build directories like `output/build-ninja-debug`, and treats Ninja configure as valid only when `build.ninja` actually exists instead of trusting a stray `CMakeCache.txt`.
- **Discovered**: The earlier Ninja failure was a wrapper-state bug as much as a toolchain issue. `output/build-ninja` only contained partial CMake metadata (`CMakeCache.txt` plus `CMakeFiles/`) with no `build.ninja`, but the wrapper skipped reconfigure because it saw the cache file. That guaranteed repeated `ninja: error: loading 'build.ninja'` failures on every subsequent build attempt.
- **Current Blocker**: Re-run `.\build.ps1 -Action smart -OutputBackend ninja` and validate that the new per-config Ninja directory (`output/build-ninja-debug`) produces a real `build.ninja` and compiles `ps2_game` incrementally. If it still fails, capture the fresh build log from `tools/ai/artifacts/build-logs/`.
- **Actions**: Verified that the fixed Ninja backend now succeeds: `tools/ai/artifacts/build-logs/20260322_030747_debug_ps2_game.log` reaches `8707/8707` and links `output/Debug/ps2_game.exe`. Reviewed the next runtime log and confirmed the game still stalls in the async file-service path around `OpenPlatformFile("GLOBAL\\DYNTEX.BIN")`, `bServiceFileSystem__Fv_0x1f8ae8`, and `bIsAsyncDone__FP5bFile_0x1f9938`. Added targeted ALPHA-only diagnostics in `ps2xRuntime/src/lib/game_overrides.cpp` for `BeginRead__10QueuedFile_0x181d20` and `bServiceFileSystem__Fv_0x1f8ae8`, plus constructor logging in `ps2xRuntime/src/lib/ps2_stubs.cpp` for `__10AsyncEntryP5bFilePFP5bFileP10AsyncEntry_i`.
- **Discovered**: The async pending-count decrement path already exists in generated `_$_10AsyncEntry_0x1f8a20`, so the likely fault is not the counter field itself. The unresolved issue is that one or more async entries servicing `GLOBAL\\DYNTEX.BIN` never reach cleanup, leaving `bFile+0x74` nonzero and trapping the game in the `bServiceFileSystem` / `bIsAsyncDone` loop.
- **Current Blocker**: Rebuild `ps2_runtime`, rerun the game, and inspect the new `[AsyncEntry:ctor]`, `[nfs-hp2-alpha] BeginRead ...`, and `[nfs-hp2-alpha] bServiceFileSystem:before/after ...` lines in `output/Debug/log.log`. The next fix depends on which async entry/state/proc combination never drains.
- **Actions**: Fixed the clean runtime build break by declaring the generated `BeginRead__10QueuedFile_0x181d20` and `bServiceFileSystem__Fv_0x1f8ae8` entry points used by the new ALPHA wrappers in `ps2xRuntime/src/lib/game_overrides.cpp`. Rebuilt with `.\build.ps1 -Action runtime -OutputBackend ninja`, which produced `tools/ai/artifacts/build-logs/20260322_034242_debug_ps2_runtime.log` and `tools/ai/artifacts/build-logs/20260322_034247_debug_ps2_game.log`. Ran a timed harness against `output/Debug/ps2_game.exe F:\Games\ISO\PS2\ALPHA\SLUS_203.62`, captured `output/Debug/raw_log.txt`, and then corrected `nfsHp2AlphaServiceFileSystem` so it logs live sentinel `next/prev` links instead of a cached head pointer.
- **Discovered**: The new binary confirms the async read path is live: `OpenPlatformFile("GLOBAL\\DYNTEX.BIN")` is followed by `[AsyncEntry:ctor] entry=0x1fea6cc file=0x1fea640 proc=0x1fadc0 pendingAfter=0x1 ...`, so the stall is on the `AsyncRealCDRead` path rather than the queued-file wrapper. The first queue-service dump was misleading because it only captured a single `head` value; after insertion it often printed `head=0x0` while another queue link still referenced `0x1fea6cc`, so the service wrapper needed one more correction before it can identify whether the head link is corrupt or simply being read from the wrong slot.
- **Current Blocker**: Rebuild once more with the corrected sentinel-link logging in `nfsHp2AlphaServiceFileSystem`, rerun, and inspect the actual queue node fields for the live `0x1fadc0` entry. The next fix depends on whether the async queue head is corrupt or `AsyncRealCDRead` is looping on a valid state transition.
- **Actions**: Reviewed `build.ps1` smart-build routing after a repeated `.\build.ps1 -Action smart -OutputBackend ninja` still triggered a large output rebuild. Confirmed that `output/build-ninja-debug` contained generated probe files (`CMakeCXXCompilerId.cpp`, `foo.h`) and patched the smart-build exclusion list to ignore `output/build-ninja-debug`, `output/build-ninja-release`, `output/build-ninja-relwithdebinfo`, and `output/build-ninja-minsizerel`.
- **Discovered**: The smart-build scan was still treating files under the active Ninja build directory as potential `output` sources. That could force unnecessary output rebuilds even when it was not the first Ninja build. This is separate from the legitimate `runtime lib is newer than ps2_game.exe` relink path, which should still trigger an output rebuild when runtime changed.
- **Current Blocker**: Re-run `.\build.ps1 -Action smart -OutputBackend ninja` after this exclusion fix and verify that repeated Ninja builds stay incremental unless `ps2_runtime.lib` or real `output` sources changed.
- **Actions**: Read the generated async queue code around `bReadAsync__FP5bFilePviPFP5bFilei_vi_0x1f9778`, `_GLOBAL_$I$bFileSlotPool_0x1f9d18`, and `bServiceFileSystem__Fv_0x1f8ae8`, then patched the ALPHA runtime wrappers in `ps2xRuntime/src/lib/game_overrides.cpp` to normalize the async-entry sentinel at `gp-0x4A98` before both `BeginRead__10QueuedFile_0x181d20` and `bServiceFileSystem__Fv_0x1f8ae8`. The wrapper now repairs zeroed head/tail links back to the sentinel or recovers the head from the existing tail link, and logs one-shot repair markers before continuing into generated code.
- **Discovered**: The async stall is no longer just “`GLOBAL\\DYNTEX.BIN` never finishes”. The generated logic expects the async-entry queue head at `gp-0x4A98` and the tail cell at `gp-0x4A94` to start as the sentinel address. The latest logs showed the first inserted entry only linked from the tail side (`sentinelNext=0x0`, `sentinelPrev=entry`, `entry.next=sentinel`, `entry.prev=0x0`), which prevents `bServiceFileSystem` from ever dispatching `AsyncRealCDRead`.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun so `output/Debug/log.log` can confirm whether the new repair fires (`[nfs-hp2-alpha] repaired async-entry queue ...`) and whether the service loop finally sees a nonzero head entry and drains the `AsyncRealCDRead` work item.
- **Actions**: Fixed a staged-build wrapper bug in `build.ps1`. `-Action runtime` was still wired to `Invoke-RuntimeAndOutputBuild`, so even after a successful `smart -OutputBackend ninja` run it would trigger another full `ps2_game` rebuild. Added a dedicated `Invoke-RuntimeBuild` path and changed the menu/runtime action to build `ps2_runtime` only.
- **Discovered**: The repeated full rebuild after `.\build.ps1 -Action runtime -OutputBackend ninja` was not expected behavior from Ninja itself. It was caused by the wrapper semantics: `runtime` literally meant `runtime + output`.
- **Current Blocker**: Validate the corrected wrapper flow. After the fix, `.\build.ps1 -Action runtime -OutputBackend ninja` should touch only `out/build` for `ps2_runtime`; use `smart` when you want automatic `runtime + output` routing.
- **Actions**: Hardened `build.ps1` artifact validation. The output build path now throws if `output\\<Config>\\ps2_game.exe` is still missing after a successful CMake/Ninja exit code, and the `smart` no-op path now also asserts that the expected executable still exists before printing `no build needed`.
- **Discovered**: The previous wrapper could still report a completed build if the backend returned success but the expected runner artifact was absent, or if `smart` reached the `none` decision while the executable had been manually deleted.
- **Current Blocker**: Re-run `.\build.ps1 -Action smart -OutputBackend ninja` after deleting `output\\Debug\\ps2_game.exe` once more. It should now fail loudly instead of reporting completion without the executable.
- **Actions**: Reviewed the latest `output/Debug/log.log` after the async-entry queue repair. Confirmed that the repair fired once at startup, then the runtime began draining real async reads for `GLOBAL\\DYNTEX.BIN`, `GLOBAL\\HUDTEX.BIN`, and `CARS\\TEXTURES.BIN`. Patched `ps2xRuntime/src/lib/ps2_stubs.cpp` so `__dl__10AsyncEntryPv`, `__dl__5bFilePv`, and `__dl__10QueuedFilePv` return as harmless no-op deletes instead of TODO traps, and added an ALPHA `ServiceQueuedFiles__Fv_0x181e10` wrapper in `ps2xRuntime/src/lib/game_overrides.cpp` to normalize the queued-file sentinel at `gp-0x57E0` and log the active queue head before the sort/service path runs.
- **Discovered**: The old async queue bug is resolved. `log.log` now shows healthy async head/tail links and live `AsyncRealCDRead` execution. The new parked PC is `0x182260` (`SortByPriority__10QueuedFileP10QueuedFileT1`), which means the next likely corruption boundary is the queued-file list/sort path rather than `bServiceFileSystem`.
- **Current Blocker**: Rebuild `ps2_runtime` and rerun so the next `log.log` shows the queued-file head diagnostics from `ServiceQueuedFiles`. The next fix depends on whether the queued-file sentinel/list is broken in the same way as the async queue or whether a bad priority/list node is trapping the sort comparator.
- **Actions**: Added an explicit `runtime-output` action to `build.ps1`. `runtime` remains a compile-only path for `ps2_runtime.lib`, while `runtime-output` now clearly means “rebuild `ps2_runtime` and relink `ps2_game.exe`”. The menu was updated to expose both choices.
- **Discovered**: The earlier guidance to use `runtime` before rerunning was wrong for changes that must land in `ps2_game.exe`. That action no longer relinks the runner by design, so using it alone guarantees an old exe even when the runtime library is fresh.
- **Current Blocker**: Use `.\build.ps1 -Action runtime-output -OutputBackend ninja` for runtime changes that need to be tested immediately in the executable. The latest queued-file and delete-stub patches will not show up in logs until the exe is relinked.
- **Actions**: Fixed the output-wrapper behavior for Ninja incremental builds. `Invoke-OutputBuild()` no longer uses `--clean-first` on the Ninja backend when `ps2_runtime.lib` is newer than `ps2_game.exe` or when the exe is missing. The Ninja path now requests a normal incremental target build/relink instead of deleting runner artifacts and forcing all `ps2_game_chunk_*` objects to rebuild.
- **Discovered**: The chunked-output optimization was being neutralized by the wrapper, not by Ninja itself. The script’s forced clean rebuild path was causing all output chunks to recompile from scratch even when only a runtime relink was needed.
- **Current Blocker**: Re-run `.\build.ps1 -Action runtime-output -OutputBackend ninja` and verify that Ninja now preserves chunk objects and limits work to the necessary relink/changed objects instead of rebuilding all `ps2_game_chunk_*` targets.
- **Actions**: Reviewed the newest `output/Debug/log.log` after the async-queue and queued-file fixes. The runtime now drains async reads for `GLOBAL\\DYNTEX.BIN`, `GLOBAL\\HUDTEX.BIN`, and `CARS\\TEXTURES.BIN`, then spends seconds in `GetLoadedTexture__11TexturePackUi_0x180b30` / `GetTextureInfo__FUii_0x180650`. Added an ALPHA override in `ps2xRuntime/src/lib/game_overrides.cpp` that builds a host-side index for the attached `TexturePack` table (`this+0x60`, `this+0x64`) and replaces the generated linear scan with an indexed lookup that preserves the original `0x8C/0x3C/0x90` validity checks.
- **Discovered**: The latest stall is no longer in file I/O. `log.log` shows the live PC at `0x180B50` and then `0x180688`, which maps to repeated `GetLoadedTexture -> GetTextureInfo` lookups against loaded texture metadata. The generated implementation is an O(n) scan over `0xA0`-byte entries for every lookup, and after the earlier queue fixes this became the dominant startup blocker.
- **Actions**: Checked the fresh `output/Debug/ps2_register_trace.txt` after rebuilding. It already contains `replace address=0x180b30 ...` and `replace address=0x181e10 ...`, so the ALPHA override registrations are landing in the runtime table. Added a one-shot startup confirmation line in `applyNfsHp2AlphaOverrides()` plus a direct entry probe in `nfsHp2AlphaGetLoadedTexture()` to prove whether the runtime ever enters the override body during the next run.
- **Discovered**: The missing `GetLoadedTexture` logs were not caused by failed `registerFunction()` replacement. The current gap is execution visibility: we need a direct runtime-entry probe to distinguish “override registered but never called” from “override entered and immediately falling back.”
- **Actions**: Verified the new probes in `output/Debug/raw_log.txt`. The run now shows both `[game_overrides] alpha registrations ready ...` and repeated `[nfs-hp2-alpha] enter GetLoadedTexture override ...` lines. Updated `build.ps1` so `-Action run` uses a default timed run window (`RunTimeoutSeconds`, default `15`) and terminates `ps2_game.exe` automatically unless `-NoWait` is used.
- **Discovered**: `GetLoadedTexture` is being called through the override, but the caller state is invalid. The captured arguments are things like `pack=0x0`, `pack=0x2cce48`, `texture=0x8868656f`, and `table=0x74736f68` (`"host"` in ASCII), so the remaining fault is upstream caller/list corruption rather than the texture lookup implementation itself.
- **Current Blocker**: Trace the bad `TexturePack` source in `GetTextureInfo__FUii_0x180650`, especially the list walk rooted at `gp-0x5808`. The next runtime patch should target the `TexturePackList` / caller object state, not `GetLoadedTexture` itself.
- **Actions**: Completed the host texture-table fast path in `ps2xRuntime/src/lib/game_overrides.cpp` by copying both table metadata and payload windows from unpacked `ZZDATA` `.BIN` files into guest memory, caching payload windows per `TexturePack`, and resolving `TextureInfo+0x8C/+0x90` lazily from the original `0x30/0x34` offsets. Added ALPHA overrides for `GetTextureInfo__FUii_0x180650`, `ReconnectTextureTable__6eModel_0x109050`, `ReconnectAllModels__Fv_0x1094d0`, `eFindAnimationSolid__FUi_0x17ca70`, `ReconnectAllAnimations__Fv_0x17c520`, `afxUpdateTextures__Fv_0x11fc60`, and `LoadCommonRegion__6bSound_0x20cf50`. Each override now repairs zeroed list sentinels, bounds linked-list walks, drops obviously invalid nodes, or disables malformed sound common-region scans instead of letting the guest spin indefinitely.
- **Discovered**: The texture lookup loop was only one symptom. The deeper issue was a chain of poisoned runtime lists and object fields: bogus model nodes with `entryBase=0x424F4C47` (`"GLOB"`), zeroed animation/AFX list sentinels, and finally a malformed sound common-region chunk walk. After the new guards, the run advances through the earlier texture/model/animation barriers and logs one concrete sound failure: `[nfs-hp2-alpha] disabled sound common region sound=0x2c2948 state=0x1 base=0xa96100 size=0xda9c region=0x0 reason=invalid-step`.
- **Current Blocker**: The live 15-second run now lands in `bServiceFileSystem__Fv_0x1f8ae8` from `InitializeSoundDriver__Fv_0x183ea8` after the sound common-region load is disabled. The next patch should focus on the post-sound-init file-service path at `ra=0x183F70`, not on the old texture/model reconnect loops.
- **Actions**: Inspected the local OpenClaw CLI and patched `tools/ai/prompt_builder.py` so `run-qwen` now defaults to the working embedded OpenClaw invocation (`openclaw agent --local --json --agent main --message {{PROMPT_TEXT}}`). The helper now replaces `{{PROMPT_TEXT}}` directly, defaults the interactive executable to `openclaw`, and no longer pushes users into the broken prompt-file placeholder flow when using OpenClaw locally.
- **Discovered**: OpenClaw does not accept a prompt-file argument in the generic form that the original helper expected. Its working local Qwen path is the embedded `agent` subcommand, and the previous `run-qwen` prompts were surfacing implementation details (`Prompt path placeholder`) that do not match the actual CLI. The helper is now aligned with the installed local agent `main` and the configured `qwen3coder` model.
- **Actions**: Fixed a follow-up placeholder substitution bug in `tools/ai/prompt_builder.py`. `run-qwen` now always treats `{{PROMPT_PATH}}` and `{{PROMPT_TEXT}}` as distinct tokens, so the OpenClaw preset passes the actual prompt body to `--message` instead of accidentally substituting the prompt file path.
- **Actions**: Tightened the ALPHA `AddTexturesSorted__17TextureDownloaderPt6bTList1Z14eActiveTexture_0x106860` override in `ps2xRuntime/src/lib/game_overrides.cpp` so it now skips the old `AddTail` fallback when the downloader/destination list lives in scratchpad or the source list is empty. Removed ctor-side `NisManager` list insertion from `ps2xRuntime/src/lib/ps2_stubs.cpp`, added a `DrawNisObjects__FP5eView_0x186718` guard that resets/drops poisoned NIS list nodes, and added targeted probes for `BeginLoad__6bSoundPcT1_0x20c760` and `Open__12cBaseSpeakerPCcT1_0x217820`.
- **Discovered**: The new timed runs moved the active render boundary to `RenderClans -> GetPixelSize` (`0x17FF64/0x17FF74 -> 0x105B30/0x105C0C`) while keeping the screen black. The scratchpad `TextureDownloader` fallback is now contained (`AddTexturesSorted skip fallback ... downloaderScratch=1 destScratch=1 source-empty`). `BeginLoad__6bSound` enters with valid `SOUND\\SNDHDR.BIN` and `SOUND\\SNDDATA.BIN` pointers, so it is not the source of the later poisoned `bOpen(FPCc)` call. The bad open still occurs later with `pathAddr=0xAAAAAAAA`, and the strongest remaining suspects are later speaker/speech loaders rather than the initial sound-header/data open.
- **Current Blocker**: Resume from the post-sound-init bad-open path. The next focused target is the later `bOpen(FPCc)` caller that still feeds `0xAAAAAAAA`, with `Open__12cBaseSpeakerPCcT1_0x217820`, `Initialize__9cSEDWedgePcT1T1T1P5bSlot_0x1cbe98`, and related speech/sample loaders as the strongest suspects. The latest checkpoint already rules out `BeginLoad__6bSound` itself.
